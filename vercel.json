{
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; git fetch --unshallow 2>/dev/null; git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA\" 2>/dev/null || exit 1; cd $(git rev-parse --show-toplevel) && git diff \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD --quiet -- frontend/"
}
